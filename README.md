# GenevaWillBeBlue 🔵

[Français](#français) | [English](#english)

---

## Français

**GenevaWillBeBlue** est le site web centralisant les informations pour l'événement communautaire du jeu **Ingress** qui se déroulera à **Genève le 20 juin 2026**. 

Le site est actuellement **pleinement fonctionnel**. Il permet de regrouper toutes les données logistiques et stratégiques nécessaires aux agents en un seul point d'accès.

### 🚀 Fonctionnalités
* **Menu Dynamique** : Navigation générée via un fichier JSON (évolutif).
* **Centralisation** : Toutes les infos pratiques (points de ralliement, horaires) au même endroit.
* **Architecture légère** : Site rapide et optimisé pour une consultation mobile sur le terrain.

### 🛠️ Stack Technique
* **Backend** : Node.js, Express
* **Frontend** : EJS, CSS3, JavaScript
* **Gestion de données** : JSON (actuel), SQL/BDD (prévu)

### ⚙️ Installation
1. Clonez le dépôt : `git clone https://github.com/votre-compte/genevawillbeblue.git`
2. Installez les dépendances : `npm install`
3. Configurez votre port dans un fichier `.env` (ex: `PORT=3000`).
4. Lancez le serveur : `node app.js`

### 🔮 Évolutions futures
* Migration vers une base de données relationnelle (SQL).
* Implémentation de requêtes dynamiques pour la gestion des participants.

---

## English

**GenevaWillBeBlue** is the central information hub for the **Ingress** community event taking place in **Geneva on June 20, 2026**.

The project is **fully functional**, providing agents with a single point of access for all logistics and strategic data.

### 🚀 Features
* **Dynamic Menu**: Navigation powered by a JSON file (scalable).
* **Information Hub**: All practical details (meeting points, schedules) in one place.
* **Lightweight Architecture**: Fast and optimized for mobile use during the event.

### 🛠️ Tech Stack
* **Backend**: Node.js, Express
* **Frontend**: EJS, CSS3, JavaScript
* **Data Management**: JSON (current), SQL/DB (planned)

### ⚙️ Setup
1. Clone the repo: `git clone https://github.com/your-account/genevawillbeblue.git`
2. Install dependencies: `npm install`
3. Set your port in a `.env` file (e.g., `PORT=3000`).
4. Start the server: `node app.js`

### 🔮 Future Roadmap
* Migration to a relational database (SQL).
* Implementation of dynamic queries for participant management.

---

📅 **Save the date: June 20, 2026 - Geneva / Rendez-vous le 20 juin 2026 à Genève !**